// rhizome calls it a fact, but find that to be a confusing name
// because PomoDB spec calls entity, attribute, value, cause quads
// a facts instead.
// To avoid confusion, we call this a `Proposition` which is a set of
// initial assertions about a particular entity.

export { create } from '../association.js'
